# This is a basic workflow to help you get started with Actions

name: CI

# Controls when the action will run. 
on:
  # Triggers the workflow on push or pull request events but only for the master branch
  push:
    branches: [ nehabluedot-patch-1 ]
  pull_request:
    branches: [ nehabluedot-patch-1 ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest
  
   # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v2.1.5 
      - run: npm install
      
      # add SSH private key to checkout github git urls
      - name: Setup SSH Keys and known_hosts
        run: |
          SSH_PATH="$HOME/.ssh"
           mkdir -p "$SSH_PATH"
           touch "$SSH_PATH/known_hosts"
           echo 123
           
           echo "${{ secrets.DEVOPS_SSH_PRIVATE_KEY }}" > "$SSH_PATH/id_rsa"
           chmod 700 "$SSH_PATH"
           ssh-keyscan github.com >> ~/.ssh/known_hosts
           echo 234
           chmod 600 "$SSH_PATH/known_hosts"
           chmod 600 "$SSH_PATH/id_rsa"
           
           echo 345
           eval $(ssh-agent)

           echo 45
           ssh-add "$SSH_PATH/id_rsa"
  
     # Tag on ReactMinApp Github Repo to trigger downstream workflow
      - name: Tagging ReactMin App Repo to trigger downstream workflow 
        run: |
          git config --global user.email "devops@bluedot.io"
          git config --global user.name "devops"
          git remote add remote-origin git@github.com:Bluedot-Innovation/Bluedot-React-Native-Minimal-Integration.git
          git fetch remote-origin
          git stash
          oldAPI="123-oldAPI"
          echo $oldAPI
        
          git checkout -b remote-downstream remote-origin/downstream
          git tag "123"
          git checkout -b old-api-downstream remote-origin/old_api
          git tag $oldAPI
          git push remote-origin "123" $oldAPI
